/**
 * Módulo de marca HFerraz Automação.
 *
 * Ponto de entrada único para todos os tokens e metadados visuais.
 * Importe sempre daqui — nunca diretamente dos arquivos internos.
 *
 * @module brand
 *
 * @example
 * ```ts
 * import { colors, logos, brandMeta, spacing } from '@/brand'
 * ```
 */

export * from './tokens'
export * from './logos'
